# Copy to ".env". Only VITE_-prefixed vars are exposed to the browser bundle, so this is for
# PUBLIC config only - never secrets. The API base URL the client calls:
VITE_API_URL=http://localhost:5001
